let compra = Number(prompt("Escreva o número:"));
let desconto = Number(compra-(compra*0.1))

if (compra >= 100) {
    alert (`você obteve desconto de 10%, o valor total foi de R$) ${desconto}`)
}else {
}
